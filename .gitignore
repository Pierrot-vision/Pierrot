# Python
__pycache__/
*.py[cod]
*.egg-info/
.ipynb_checkpoints/

# 추론 산출물 (런타임 출력 — 저장소 제외)
results/

# step별 샘플 그리드 원본 PNG (장당 ~47MB) — 저장소에는 문서용 JPG 압축본만 올린다.
# 원본은 results/grid_*/ 의 개별 60장에서 montage 로 재조립 가능.
docs/baseprior_grid_phase*_*.png

# 모델 가중치 (체크포인트는 저장소에 올리지 않음)
*.pt
*.safetensors
*.ckpt
*.bin

# OS
.DS_Store
